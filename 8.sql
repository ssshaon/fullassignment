(SELECT DISTINCT
	SNAME
FROM
	STUDENT,
	ENROLLED,
	COURSE
WHERE
	STUDENT.SNUM = ENROLLED.SNUM
AND ENROLLED.CNUM = COURSE.CNUM)
UNION (
	SELECT
		MEETS_AT
	FROM
		COURSE
	GROUP BY
		MEETS_AT
	HAVING
		"MAX" (ROOM) = '2'
)